apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-agent-deployment
  labels:
    app: ai-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-agent
  template:
    metadata:
      labels:
        app: ai-agent
    spec:
      containers:
        - name: ai-agent
          image: sos-reg.kr.ncr.ntruss.com/ai-agent-fe:latest
          ports:
            - containerPort: 3000
          imagePullPolicy: Always
